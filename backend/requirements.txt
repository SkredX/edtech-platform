fastapi==0.137.2
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2

# NLP / clustering (local, zero API cost — spaCy's small model is light,
# unlike sentence-transformers/torch which we removed; see core/embeddings.py)
scikit-learn==1.9.0
spacy==3.8.14
numpy==1.26.4

# Chunking — lightweight splitter package only (avoids the full langchain
# dependency tree, which matters when RAM is tight on free-tier hosting)
langchain-text-splitters==0.3.6

# PDF parsing
PyPDF2==3.0.1

# Vector DB — v9 is a ground-up rewrite vs the old pinecone-client package
pinecone==9.1.0

# LLM
groq==1.4.0

# Cache
redis==8.0.0

# Misc
tqdm==4.68.3
python-multipart==0.0.32
